
weak (~window: { Dict | (and 
  (isPrivileged v)
  (implies (has v "secret") (NotUndef (sel v "secret")))
  (implies (has v "public") (not (isSecret (sel v "public"))))
  )} > lWinPro)

(*val theWinPro :: { Dict | (and *)
(*  (implies (has v "p") (isSecret (sel v "p")))*)
(*  )}*)

heap (
  lWinPro : theWinPro > lROOT
)
